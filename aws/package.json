{
  "name": "player-name-service",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "npm i && sls deploy",
    "deploy:production": "npm i && sls deploy --stage production"
  },
  "dependencies": {
    "uuid": "^8.3.2"
  }
}
